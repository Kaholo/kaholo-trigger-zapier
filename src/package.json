{
  "name": "kaholo-trigger-zapier",
  "version": "1.0.0",
  "description": "Handles Zapier webhooks",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "dependencies": {
    "minimatch": "^3.0.4"
  }
}
